{
  "name": "flowforge-ai",
  "private": true,
  "description": "Repo-root tooling only. Playwright lives here rather than in frontend/ because the gates under tests/phase6/ drive the whole stack, not the SPA package (D21 decision 4). The application itself is frontend/.",
  "scripts": {
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
